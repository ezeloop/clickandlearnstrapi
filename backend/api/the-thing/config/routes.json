{
  "routes": [
    {
      "method": "GET",
      "path": "/the-things",
      "handler": "the-thing.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/the-things/count",
      "handler": "the-thing.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/the-things/:id",
      "handler": "the-thing.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/the-things",
      "handler": "the-thing.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/the-things/:id",
      "handler": "the-thing.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/the-things/:id",
      "handler": "the-thing.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
